rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Test-only permissive rules used by the Firestore Emulator.
    // Do NOT deploy these to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}